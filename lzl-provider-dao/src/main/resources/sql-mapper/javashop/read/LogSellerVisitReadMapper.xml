<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lzl.dao.javashop.read.LogSellerVisitReadDao">

	<sql id="Base_Column_List" >
		member_id,seller_id,create_time 
	</sql>

    <select id="getById" parameterType="Integer" resultType="com.lzl.bean.javashop.LogSellerVisit">
        select <include refid="Base_Column_List" />
		from log_seller_visit
		where  
    </select>

    <select id="get" parameterType="java.util.Map" resultType="com.lzl.bean.javashop.LogSellerVisit">
        select <include refid="Base_Column_List" />
		from log_seller_visit
        <include refid="Where_Clause"/>
		<if test="size != null and size &gt; 0">limit #{start},#{size}</if>
    </select>
	
	<select id="getCount" parameterType="java.util.Map" resultType="Integer">
        select count(*)
		from log_seller_visit
        <include refid="Where_Clause"/>
    </select>
	
	<sql id="Where_Clause">
        <where>
			<if test="memberId != null and memberId !=''">
			and member_id = #{memberId}
			</if>
			<if test="sellerId != null and sellerId !=''">
			and seller_id = #{sellerId}
			</if>
			<if test="createTime != null and createTime !=''">
			and create_time = #{createTime}
			</if>
        </where>
    </sql>

</mapper>