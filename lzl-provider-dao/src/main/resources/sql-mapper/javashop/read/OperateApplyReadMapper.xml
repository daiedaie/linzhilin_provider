<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lzl.dao.javashop.read.OperateApplyReadDao">

	<sql id="Base_Column_List" >
		operate_apply_id,user_type,apply_user_id,operate_type,content,status,feedback,update_time,create_time 
	</sql>

    <select id="getById" parameterType="Integer" resultType="com.lzl.bean.javashop.OperateApply">
        select <include refid="Base_Column_List" />
		from operate_apply
		where operate_apply_id = #{operateApplyId} 
    </select>

    <select id="get" parameterType="java.util.Map" resultType="com.lzl.bean.javashop.OperateApply">
        select <include refid="Base_Column_List" />
		from operate_apply
        <include refid="Where_Clause"/>
		<if test="size != null and size &gt; 0">limit #{start},#{size}</if>
    </select>
	
	<select id="getCount" parameterType="java.util.Map" resultType="Integer">
        select count(*)
		from operate_apply
        <include refid="Where_Clause"/>
    </select>
	
	<sql id="Where_Clause">
        <where>
			<if test="operateApplyId != null and operateApplyId !=''">
			and operate_apply_id = #{operateApplyId}
			</if>
			<if test="userType != null and userType !=''">
			and user_type = #{userType}
			</if>
			<if test="applyUserId != null and applyUserId !=''">
			and apply_user_id = #{applyUserId}
			</if>
			<if test="operateType != null and operateType !=''">
			and operate_type = #{operateType}
			</if>
			<if test="content != null and content !=''">
			and content = #{content}
			</if>
			<if test="status != null and status !=''">
			and status = #{status}
			</if>
			<if test="feedback != null and feedback !=''">
			and feedback = #{feedback}
			</if>
			<if test="updateTime != null and updateTime !=''">
			and update_time = #{updateTime}
			</if>
			<if test="createTime != null and createTime !=''">
			and create_time = #{createTime}
			</if>
        </where>
    </sql>

</mapper>