<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lzl.dao.javashop.write.CartWriteDao">
	
	<insert id="post">
		insert into cart (id,create_time,member_id,count,spec_id,spec_info,product_id,seller_id,product_goods_id,checked )
		values (#{id},#{createTime},#{memberId},#{count},#{specId},#{specInfo},#{productId},#{sellerId},#{productGoodsId},#{checked})
	</insert>

    <update id="put">
        update cart
        <set>
				id = #{id},
				create_time = #{createTime},
				member_id = #{memberId},
				count = #{count},
				spec_id = #{specId},
				spec_info = #{specInfo},
				product_id = #{productId},
				seller_id = #{sellerId},
				product_goods_id = #{productGoodsId},
				checked = #{checked},
        </set>
        where id = #{id} 
    </update>

</mapper>