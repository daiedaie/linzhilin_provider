<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lzl.dao.javashop.write.PromotionWriteDao">
	
	<insert id="post">
		insert into promotion (promotion_id,parent_id,seller_id,name,type,content,status,start_time,end_time,reg_deadline,user_type,user_id,description,create_time )
		values (#{promotionId},#{parentId},#{sellerId},#{name},#{type},#{content},#{status},#{startTime},#{endTime},#{regDeadline},#{userType},#{userId},#{description},#{createTime})
	</insert>

    <update id="put">
        update promotion
        <set>
				promotion_id = #{promotionId},
				parent_id = #{parentId},
				seller_id = #{sellerId},
				name = #{name},
				type = #{type},
				content = #{content},
				status = #{status},
				start_time = #{startTime},
				end_time = #{endTime},
				reg_deadline = #{regDeadline},
				user_type = #{userType},
				user_id = #{userId},
				description = #{description},
				create_time = #{createTime},
        </set>
        where promotion_id = #{promotionId} 
    </update>

</mapper>